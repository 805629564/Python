使用方括号[]来表示列表

方法与函数的使用规则：方法  对象名.方法名();函数：  函数名(对象名)

方法pop():
    方法pop()可以将列表尾元素删除，并且直接拿来用
    使用pop（i）可以将指定位置处的元素删除，并直接拿来用

方法remove()：
    根据元素的值来删除元素，用在不知道元素所处位置的情况 方法remove()，()内为所要删除的元素的值
    方法remove（）同样可以将删除的值拿来用
    remove()只能删除第一个指定的值，如果要重复删除，可以用循环的方式来做

方法pop()与方法remove()区别:
    主要是使用方法上的区别：pop()可以根据索引删除，remove()根据元素的值删除
    
对列表进行排序：
    （1）永久性排序--方法sort():按照元素第一个字符的ASCII码的大小进行排序
    （2）临时排序--函数sorted()
    
确定列表长度：使用函数len(对象名)

关于使用列表索引的一些知识：
    访问最后一个列表元素，使用索引-1

Problem List:
    （1）方法可以永久改变元素，函数只能临时改变元素的值？
