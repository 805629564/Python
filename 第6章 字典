字典就是一系列的键-值对，可以通过键访问与之相关联的值

Python中的任何对象都可以作为字典中的值

关于括号：
    列表--[]
    元组--()
    字典--{}
    
遍历字典时，键-值对的返回顺序与存储顺序可能不同。应为Python不关心键-值对的存储顺序，只跟踪键与值之间的关联关系

遍历所有的键-值对：for key,value in user_0.items():

遍历所有的键：for name in favorite_languages.keys():

键、值均可以直接被访问，格式为：字典名.keys()   字典名.values()

集合（set）可以剔除重复项  示例：set(对象)
  
嵌套只是 值 嵌套，键不会嵌套
