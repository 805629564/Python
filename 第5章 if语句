使用 and 和 or 来连接两个表达式，表达式可以不用加括号，但如果加了括号会增加可读性
if-elif-else

Python并不要求if语句内一定要有else，不用else而只用elif可以让读者看起来更清晰

在Python中，将列表名用在条件表达式中，列表但凡有元素就返回True，一个元素都没有就返回False。
if 列表名：     可以用来判断列表是否为空

PEP8建议在比较运算符（==、>=、<=）两边各添加一个空格
